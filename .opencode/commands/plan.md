---
description: Switch to the plan-feature agent and produce an implementation plan from a spec
agent: Plan feature
---

Produce an implementation plan for the feature described in
`.opencode/specs/spec.md`. If no spec exists, ask the user to describe
the feature or run `/requirements` first.

Then follow the full planning process: explore the codebase, ask
clarifications, write the plan inline, and save it to
`.opencode/specs/plan.md`.

$ARGUMENTS
