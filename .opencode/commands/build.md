---
description: Switch to the Build Agent and implement a feature from a plan
agent: Build Agent
---

Implement the feature described in `.opencode/specs/plan.md`. If no plan
exists, check `.opencode/specs/spec.md` and derive a plan from that. If
neither exists, ask the user what to build.

Then follow the full workflow: analyze, plan, apply, update related files,
validate, and report.

$ARGUMENTS
