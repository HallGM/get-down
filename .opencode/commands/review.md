---
description: Run AI code review across all uncommitted changes
agent: review-lead
---

Review the current uncommitted changes.

If `$ARGUMENTS` contains a PR description or requirements, use them.
Otherwise check `.opencode/specs/spec.md` for context (if it exists).

Run the full multi-persona review process.

$ARGUMENTS
